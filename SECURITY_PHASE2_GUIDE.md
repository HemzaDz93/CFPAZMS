# 🔐 المرحلة 2 من نظام الأمان - دليل شامل
# Security Phase 2 - Comprehensive Guide

## 📋 نظرة عامة
هذه المرحلة تقدم نظام أمان متقدم وشامل يتضمن:
- سياسة كلمات مرور قوية وقابلة للتخصيص
- إدارة جلسات آمنة
- حماية من الهجمات (SQL Injection, XSS, CSRF)
- تحديد معدل الطلبات (Rate Limiting)
- نظام تنبيهات الأمان
- تسجيل وتدقيق شامل
- إدارة مفاتيح الوصول (API Keys)
- قائمة IP الموثوقة

---

## 🔧 الإعدادات الأمنية (config.py)

### سياسة كلمات المرور
```python
PASSWORD_MIN_LENGTH = 12                    # الطول الأدنى
PASSWORD_REQUIRE_UPPERCASE = True           # الأحرف الكبيرة إجبارية
PASSWORD_REQUIRE_LOWERCASE = True           # الأحرف الصغيرة إجبارية
PASSWORD_REQUIRE_NUMBERS = 1                # عدد الأرقام المطلوبة
PASSWORD_REQUIRE_SPECIAL_CHARS = 1          # عدد الأحرف الخاصة المطلوبة
PASSWORD_EXPIRY_DAYS = 90                   # انتهاء صلاحية كلمة المرور
PASSWORD_HISTORY_COUNT = 5                  # منع إعادة استخدام آخر 5 كلمات
```

### إدارة الجلسات
```python
SESSION_TIMEOUT_MINUTES = 30                # مدة انتهاء الجلسة
MAX_SESSIONS_PER_USER = 5                   # أقصى جلسات نشطة لكل مستخدم
SESSION_COOKIE_SECURE = True                # HTTPS فقط (في الإنتاج)
SESSION_COOKIE_HTTPONLY = True              # منع وصول JavaScript
SESSION_COOKIE_SAMESITE = 'Lax'             # حماية CSRF
```

### تحديد معدل الطلبات
```python
RATE_LIMIT_ENABLED = True
RATE_LIMIT_DEFAULT_PER_MINUTE = 60          # 60 طلب/دقيقة
```

### الأمان العام
```python
LOGIN_ATTEMPT_LIMIT = 5                     # محاولات دخول قبل القفل
LOGIN_LOCKOUT_DURATION_MINUTES = 30         # مدة القفل بالدقائق
MFA_ENABLED = False                         # المصادقة الثنائية
MFA_REQUIRED_FOR_ADMINS = True              # إجبارية للمسؤولين
IP_WHITELIST_ENABLED = False                # قائمة IP الموثوقة
ENCRYPTION_ENABLED = True                   # تشفير البيانات
AUDIT_LOGGING_ENABLED = True                # سجل التدقيق
```

---

## 📁 النماذج (Models)

### 1. PasswordPolicy - سياسة كلمات المرور
```python
# جدول يحتوي على إعدادات سياسة كلمات المرور
- min_length: الطول الأدنى
- require_uppercase: الأحرف الكبيرة
- require_lowercase: الأحرف الصغيرة
- require_numbers: عدد الأرقام
- require_special_chars: عدد الأحرف الخاصة
- expiry_days: انتهاء الصلاحية
- history_count: عدد كلمات المرور السابقة المحفوظة
- lockout_threshold: عدد محاولات الدخول الفاشلة
```

### 2. PasswordHistory - سجل كلمات المرور
```python
# تتبع كلمات المرور القديمة لمنع إعادة الاستخدام
- user_id: معرف المستخدم
- password_hash: تجزئة كلمة المرور
- changed_at: تاريخ التغيير
- changed_by: من قام بالتغيير (IP)
```

### 3. SessionToken - رموز الجلسات
```python
# إدارة الجلسات النشطة
- user_id: معرف المستخدم
- token: الرمز الفريد
- token_hash: تجزئة الرمز
- ip_address: عنوان IP
- user_agent: معلومات المتصفح
- device_fingerprint: بصمة الجهاز
- expires_at: انتهاء الصلاحية
- last_activity: آخر نشاط
- is_active: الجلسة نشطة؟
- revoked_at: تاريخ الإلغاء
```

### 4. SecurityAlert - تنبيهات الأمان
```python
# تسجيل الأحداث الأمنية المهمة
- user_id: المستخدم المتأثر
- alert_type: نوع التنبيه (login_failed, account_lockout, etc.)
- severity: مستوى الخطورة (low, medium, high, critical)
- title: عنوان التنبيه
- description: تفاصيل التنبيه
- ip_address: عنوان IP
- user_agent: المتصفح
- is_acknowledged: هل تم الإقرار به
```

### 5. IPWhitelist - قائمة IP الموثوقة
```python
# إضافة عناوين IP موثوقة للمستخدم
- user_id: معرف المستخدم
- ip_address: عنوان IP
- device_name: اسم الجهاز
- device_fingerprint: بصمة الجهاز
- is_active: هل النقطة نشطة
- expires_at: تاريخ انتهاء الصلاحية
```

### 6. AccessKey - مفاتيح الوصول
```python
# مفاتيح API للوصول البرمجي
- user_id: معرف المستخدم
- key_name: اسم المفتاح
- key_hash: تجزئة المفتاح
- key_prefix: آخر أحرف المفتاح للعرض
- permissions: صلاحيات المفتاح (JSON)
- is_active: هل المفتاح نشط
- expires_at: انتهاء الصلاحية
- revoked_at: تاريخ الإلغاء
```

### 7. SecurityLog - سجل الأمان
```python
# تسجيل تفصيلي لجميع الإجراءات الأمنية
- user_id: المستخدم
- action: نوع الإجراء
- resource_type: نوع المورد المتأثر
- resource_id: معرف المورد
- old_values: القيم القديمة (JSON)
- new_values: القيم الجديدة (JSON)
- ip_address: عنوان IP
- user_agent: المتصفح
- status: حالة الإجراء
- error_message: رسالة الخطأ (إن وجدت)
- timestamp: وقت الإجراء
```

### 8. RateLimitRule - قواعس معدل الطلبات
```python
# قواعس تحديد معدل الطلبات
- endpoint: المسار (مثل /auth/login)
- method: طريقة HTTP
- requests_per_minute: الطلبات المسموحة في الدقيقة
- requests_per_hour: الطلبات المسموحة في الساعة
- bypass_roles: الأدوار المستثناة
- is_enabled: هل القاعدة مفعلة
```

---

## 🔐 الخدمات الأمنية (security_services.py)

### PasswordValidator - مدقق كلمات المرور
```python
# التحقق من قوة كلمات المرور
PasswordValidator.get_policy()                # الحصول على السياسة الحالية
PasswordValidator.validate_password(pwd)     # التحقق من قوة كلمة المرور
PasswordValidator.check_password_history()   # التحقق من التاريخ
PasswordValidator.set_password(user, pwd)    # تعيين كلمة مرور جديدة
```

### SessionManager - مدير الجلسات
```python
# إدارة جلسات المستخدمين
SessionManager.create_session()              # إنشاء جلسة جديدة
SessionManager.validate_session()            # التحقق من صحة الجلسة
SessionManager.revoke_session()              # إلغاء جلسة واحدة
SessionManager.revoke_all_sessions()         # إلغاء جميع الجلسات
```

### SecurityAlertService - خدمة التنبيهات
```python
# إدارة تنبيهات الأمان
SecurityAlertService.create_alert()          # إنشاء تنبيه جديد
SecurityAlertService.acknowledge_alert()     # الإقرار بالتنبيه
SecurityAlertService.get_critical_alerts()   # الحصول على التنبيهات الحرجة
```

### IPSecurityManager - مدير أمان IP
```python
# إدارة عناوين IP
IPSecurityManager.is_ip_whitelisted()        # التحقق من الموثوقية
IPSecurityManager.add_to_whitelist()         # إضافة عنوان موثوق
IPSecurityManager.detect_suspicious_login()  # كشف محاولات مريبة
```

### AccessKeyManager - مدير مفاتيح الوصول
```python
# إدارة مفاتيح API
AccessKeyManager.create_key()                # إنشاء مفتاح جديد
AccessKeyManager.verify_key()                # التحقق من صحة المفتاح
AccessKeyManager.revoke_key()                # إلغاء مفتاح
```

---

## 🛡️ Middleware الأمان (security_middleware.py)

### SecurityHeaders - رؤوس الأمان
```
X-Frame-Options: SAMEORIGIN              # منع Click-jacking
X-Content-Type-Options: nosniff          # منع MIME-sniffing
X-XSS-Protection: 1; mode=block          # حماية XSS
Content-Security-Policy: ...             # سياسة محتوى أمان
Strict-Transport-Security: ...           # إجبار HTTPS
```

### RateLimitMiddleware - تحديد معدل الطلبات
```python
# فحص معدل الطلبات والحد من الإساءة
- يحسب الطلبات لكل IP
- يحظر الطلبات الزائدة
- يسجل الانتهاكات
```

### InputSanitizer - تنظيف المدخلات
```python
# تنظيف البيانات من الهجمات XSS
- إزالة السكريبتات الضارة
- إزالة الأحداث المضمنة
- تنظيف HTML
```

### SQLInjectionProtection - حماية من SQL Injection
```python
# الكشف عن محاولات حقن SQL
- البحث عن الكلمات المفتاحية الخطرة
- حجب الطلبات المريبة
- تسجيل المحاولات
```

---

## 🚀 المسارات الأمنية (routes/security_advanced.py)

### مسارات سياسة كلمات المرور
```
GET/POST /security/password-policy    # إدارة سياسة كلمات المرور
```

### مسارات تنبيهات الأمان
```
GET     /security/alerts              # عرض التنبيهات
POST    /security/alerts/<id>/acknowledge  # الإقرار بالتنبيه
```

### مسارات مفاتيح الوصول
```
GET     /security/access-keys         # عرض المفاتيح الشخصية
GET/POST /security/access-keys/create  # إنشاء مفتاح جديد
POST    /security/access-keys/<id>/revoke # إلغاء مفتاح
```

### مسارات قائمة IP الموثوقة
```
GET     /security/trusted-ips         # عرض القائمة
GET/POST /security/trusted-ips/add     # إضافة عنوان
POST    /security/trusted-ips/<id>/remove # إزالة عنوان
```

### مسارات سجل التدقيق
```
GET     /security/audit-log           # عرض السجل الأماني
```

### مسارات إدارة معدل الطلبات
```
GET     /security/rate-limits         # عرض القواعد
GET/POST /security/rate-limits/create  # إنشاء قاعدة جديدة
GET/POST /security/rate-limits/<id>/edit # تعديل قاعدة
```

### مسارات لوحة التحكم
```
GET     /security/dashboard           # لوحة تحكم الأمان
```

### مسارات API الأمان
```
POST    /security/api/password-strength    # فحص قوة كلمة المرور
GET     /security/api/session-info         # معلومات الجلسة
```

---

## 📊 الإحصائيات والتقارير

### لوحة تحكم الأمان تعرض:
- إجمالي عدد المستخدمين
- محاولات الدخول الفاشلة (24 ساعة)
- التنبيهات اليومية
- التنبيهات الحرجة غير المُقرّ بها
- آخر التنبيهات الأمنية
- محاولات الدخول الفاشلة الأخيرة
- توزيع التنبيهات حسب النوع

---

## 🔐 أفضل الممارسات الأمنية

### 1. كلمات المرور
- استخدم كلمات مرور قوية (12+ حرف)
- غيّر كلمة المرور كل 90 يوم
- لا تعيد استخدام كلمات مرور قديمة
- استخدم مصادقة ثنائية (2FA)

### 2. الجلسات
- استخدم HTTPS في الإنتاج
- قم بتسجيل الخروج عند الانتهاء
- راقب جلساتك النشطة
- أضف عناوين IP الموثوقة

### 3. المفاتيح
- احفظ المفاتيح في مكان آمن
- استخدم مفاتيح منفصلة لكل تطبيق
- أعد تدوير المفاتيح بانتظام
- ألغِ المفاتيح غير المستخدمة

### 4. المراقبة
- راقب لوحة تحكم الأمان بانتظام
- استجب للتنبيهات الحرجة فوراً
- راجع سجل التدقيق مرة أسبوعية
- احتفظ بسجلات الأمان لمدة سنة على الأقل

### 5. النسخ الاحتياطية
- أنشئ نسخ احتياطية منتظمة
- احفظ النسخ الاحتياطية في مكان آمن
- اختبر استعادة النسخ الاحتياطية
- استخدم التشفير للنسخ الاحتياطية

---

## 🚀 التهيئة

### تشغيل سكريبت التهيئة:
```bash
python init_security_phase2.py
```

### التحديثات اليدوية للـ .env:
```env
# Password Policy
PASSWORD_MIN_LENGTH=12
PASSWORD_REQUIRE_UPPERCASE=true
PASSWORD_REQUIRE_LOWERCASE=true
PASSWORD_REQUIRE_NUMBERS=1
PASSWORD_REQUIRE_SPECIAL_CHARS=1
PASSWORD_EXPIRY_DAYS=90

# Session Management
SESSION_TIMEOUT_MINUTES=30
MAX_SESSIONS_PER_USER=5

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_DEFAULT_PER_MINUTE=60

# Login Security
LOGIN_ATTEMPT_LIMIT=5
LOGIN_LOCKOUT_DURATION_MINUTES=30

# MFA
MFA_ENABLED=false
MFA_REQUIRED_FOR_ADMINS=true

# Encryption
ENCRYPTION_ENABLED=true
```

---

## 🐛 استكشاف الأخطاء

### مشكلة: حساب مقفول بعد عدة محاولات دخول فاشلة
**الحل**: تحقق من إعدادات `LOGIN_ATTEMPT_LIMIT` و `LOGIN_LOCKOUT_DURATION_MINUTES`

### مشكلة: كلمات المرور الضعيفة مقبولة
**الحل**: تحقق من سياسة كلمات المرور في `/security/password-policy`

### مشكلة: أخطاء في الوصول من عناوين IP جديدة
**الحل**: أضف عنوان IP إلى قائمة الموثوقة عبر `/security/trusted-ips/add`

---

## 📚 الموارد الإضافية

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [CWE Most Dangerous](https://cwe.mitre.org/top25/)
- [NIST Cybersecurity Framework](https://www.nist.gov/cyberframework/)

---

## ✅ قائمة التحقق

- [ ] تحديث إعدادات `config.py`
- [ ] تشغيل `init_security_phase2.py`
- [ ] مراجعة سياسة كلمات المرور
- [ ] تفعيل HTTPS في الإنتاج
- [ ] إعداد OAuth (Google/Azure)
- [ ] تكوين النسخ الاحتياطية
- [ ] اختبار حماية المعدل
- [ ] مراقبة لوحة تحكم الأمان
- [ ] توثيق إجراءات الطوارئ
- [ ] تدريب المستخدمين على الأمان

---

**آخر تحديث**: 2024
**الإصدار**: Phase 2.0
**الحالة**: ✅ في الإنتاج